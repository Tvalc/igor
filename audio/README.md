# Audio drop-in

Per-variant music + SFX pack referenced by the theme pack's `audio` field. Optional at Basic Launch; the engine mutes gracefully when files are absent. Keep total build under 20MB including audio.
